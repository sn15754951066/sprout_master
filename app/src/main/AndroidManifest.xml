<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.sprout">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name="com.example.myapplication.app.MyApp"
        android:allowBackup="true"
        android:icon="@drawable/oasis"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="n">
       <activity android:name=".ui.more.ThemeActivity"/>
        <activity android:name=".ui.more.SubmitMoreActivity" />
        <activity android:name=".ui.more.TagsActivity" />
        <activity android:name=".login.LoginOkActivity" />
        <activity android:name=".login.EnterPageActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".login.InterestListActivity" />
        <activity android:name=".login.InterestActivity" />
        <activity android:name=".ui.more.MoreEditorActivity" />
        <activity android:name=".login.SexActivity" />
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>